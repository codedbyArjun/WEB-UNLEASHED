# Day 8 – Internal CSS Profile 🧠🎯

✅ Used `<style>` tag inside `<head>` for internal CSS  
✅ Styled a heading, description, and a profile card layout  
✅ Applied color, font, alignment, margin, padding, shadow, and border-radius  
✅ Used class and ID selectors properly  
✅ Embedded an image with styling  
✅ Final output ready for GitHub and presentation  
